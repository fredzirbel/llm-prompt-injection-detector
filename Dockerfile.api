FROM python:3.11-slim

WORKDIR /app

COPY pyproject.toml .
COPY src/ src/
COPY ml/model/ ml/model/

RUN pip install --no-cache-dir ".[ml]"

EXPOSE 8000

CMD ["uvicorn", "detector.app:app", "--host", "0.0.0.0", "--port", "8000"]
